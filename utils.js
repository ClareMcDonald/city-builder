// export { createCountString };

export function createCountString(waterfrontCount, skylineCount, castleCount) {

    return `You have changed the waterfront ${waterfrontCount} times, the skyline ${skylineCount} times, and the castle ${castleCount} times.`;

}